"""Inyección de dependencias: qué adaptador implementa cada puerto.

Sin framework de DI, solo funciones factory (ARQUITECTURA.md §3).

Pendiente: se rellena en el paso 4 (API FastAPI), cuando existan
``OSRMAdapter`` y el endpoint que los consume.
"""
